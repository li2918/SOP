{
  "name": "sop-site",
  "version": "1.0.0",
  "description": "运营 SOP 知识库 - 内部 SOP 查询与协作平台",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "encrypt": "node scripts/encrypt.mjs",
    "build:secure": "npm run build && npm run encrypt"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4",
    "vitepress": "^1.5.0"
  }
}
